{
  "name": "scripts",
  "private": true,
  "scripts": {
    "dev": "tsup src/*.ts --sourcemap --watch --clean",
    "build": "tsup src/*.ts --clean",
    "check:types": "tsc --noEmit",
    "download-releases": "node dist/download-releases.js"
  },
  "devDependencies": {
    "tsup": "^6.5.0",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "lib": "workspace:*"
  },
  "tsup": {
    "noExternal": [
      "lib"
    ]
  }
}
